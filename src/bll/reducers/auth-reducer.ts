type ProfileStateType ={}
type ActionType = ReturnType<any>

const authReducer = (state:ProfileStateType,action:ActionType) => {

    switch (action.type) {
        default:
            return state

    }

}


export default authReducer;