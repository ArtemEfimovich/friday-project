type ProfileStateType ={}
type ActionType = ReturnType<any>

const registrationReducer = (state:ProfileStateType,action:ActionType) => {

    switch (action.type) {
        default:
            return state

    }

}


export default registrationReducer;